function func(inputArray) {
    let array = [...inputArray];
    let dashboard = [[], [], []];
    dashboard.forEach(element => {
        element.fill(false, 0, 2);
    });
    for (const coordinates of array) {
        
    }
}
func()